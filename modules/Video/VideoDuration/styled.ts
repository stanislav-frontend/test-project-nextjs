import styled from 'styled-components';

export const StyledVideoDuration = styled.p`
  background-color: rgba(0, 0, 0, .35);
  margin: 0;
  font-size: 1.2rem;
  font-weight: 700;
  padding: .4em 1em;
  letter-spacing: .1px;
  backdrop-filter: blur(3px);
  border-top-left-radius: 10px;
`;